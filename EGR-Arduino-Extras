# Source URL
$url = "https://github.com/SamsonS-CAC/Faronics/raw/Deploy/01.3_arduino-libraries+extras.zip"

# Destation file
$dest = "c:\ITS\01.3_arduino-libraries+extras.zip"

# Download the file
Invoke-WebRequest -Uri $url -OutFile $dest

# Extract the Files
Expand-Archive -LiteralPath "$dest" -DestinationPath "c:\"
